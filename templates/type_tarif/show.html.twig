{% extends 'base.html.twig' %}

{% block title %}TypeTarif{% endblock %}

{% block body %}
    <h1>TypeTarif</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_tarif.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ type_tarif.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_tarif_index') }}">back to list</a>

    <a href="{{ path('type_tarif_edit', {'id': type_tarif.id}) }}">edit</a>

    {{ include('type_tarif/_delete_form.html.twig') }}
{% endblock %}

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success">{{ button_label|default('AJOUTER') }}  <i class="fa fa-paper-plane-o" aria-hidden="true"></i> </button>
{{ form_end(form) }}

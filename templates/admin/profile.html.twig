{% extends "base.html.twig"  %}
{% block body %} 

<div id="right-panel" class="right-panel">
        <!-- Header-->
        
        <!-- /#header -->
        <!-- Content -->
        <div class="content">
            <div class="animated fadeIn">
                <!-- Widgets  -->
                <div class="row">
                    <div class="col-md-8">
                        <aside class="profile-nav alt">
                            <section class="card">
                                <div class="card-header user-header alt twt-feed blue-bg">
                                    <div class="media">
                                        <a href="#">
                                            <img class="align-self-center rounded-circle mr-3" style="width:85px; height:85px;" alt="" src="images/admin.jpg">
                                        </a>
                                        <div class="media-body">
                                            <h2 class="text-light display-6"></h2>
                                            <p>{{  app.user.status  }}</p>
                                        </div>
                                    </div>
                                </div>


                                <ul class="list-group list-group-flush">
                                    <li class="list-group-item">
                                        <a href="#"> <i class="fa fa-envelope-o"></i> EMAIL :   <span class="badge badge-primary pull-right">{{ app.user.email   }}</span></a>
                                    </li>
                                    <li class="list-group-item">
                                        <a href="#"> <i class="fa fa-phone"></i> TELEPHONE <span class="badge badge-danger pull-right">{{ app.user.telephone }}</span></a>
                                    </li>
                                    <li class="list-group-item">
                                        <a href="#"> <i class="fa fa-plus-square-o"></i> Numero de siret <span class="badge badge-success pull-right">{{ app.user.NumeroSiret	}}</span></a>
                                    </li>
                                    <li class="list-group-item">
                                        <a href="#"> <i class="fa fa-comments-o"></i> Message <span class="badge badge-warning pull-right r-activity">03</span></a>
                                    </li>
                                </ul>

                            </section>
                        </aside>
                    </div>
                    
                </div>
                
            </div>
            <!-- .animated -->
            
        </div>
</div> 
{% endblock %}
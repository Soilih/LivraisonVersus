{% extends 'base.html.twig' %}
{% block body %}
 <div id="right-panel" class="right-panel">
    <div class="content">
        <div class="animated fadeIn">
            <!-- Widgets  -->
            <div class="row">
                <div class="col-md-7">
                    <div class="card">
                        <div class="card-header bg-primary mb-3">
                            <strong class="card-title color-white">GESTION Tarif de vehicule  </strong>
                        </div>
                        <div class="card-body">
                            <table class="table table-bordered ">
                               <thead>
                                    <tr>
                                        <th>Id</th>
                                        <th>Prix</th>
                                          <th>Type de tarif </th>

                                        <th>actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                {% for tarif_vehicule in tarif_vehicules %}
                                    <tr>
                                        <td>{{ tarif_vehicule.id }}</td>
                                        <td>{{ tarif_vehicule.prix }} € </td>
                                        <td>{{ tarif_vehicule.typeTarif.libelle }} KG </td>
                                        <td>
                                            <a href="{{ path('tarif_vehicule_show', {'id': tarif_vehicule.id}) }}">show</a>
                                            <a href="{{ path('tarif_vehicule_edit', {'id': tarif_vehicule.id}) }}">edit</a>
                                             {{ include('tarif_vehicule/_delete_form.html.twig') }}
                                        </td>
                                    </tr>
                                {% else %}
                                    <tr>
                                        <td colspan="3">no records found</td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>
                            <a class="btn btn-success btn-sm" href="{{ path('pays_new') }}">Creer un nouveau tarif  </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
 </div>
</div>  
{% endblock %}

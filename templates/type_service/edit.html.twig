{% extends 'base.html.twig' %}
{% block body %}

 <div id="right-panel" class="right-panel">
    <div class="content">
        <div class="animated fadeIn">
            <!-- Widgets  -->
            <div class="row">
                <div class="col-md-7">
                    <div class="card">
                        <div class="card-header bg-primary mb-3 text-white">
                            <strong class="card-title">MODIFIER LE TYPE DE SERVICE  </strong>
                        </div>
                        <div class="card-body">
                            {{ include('type_service/_form.html.twig', {'button_label': 'modifier'}) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
 </div>
</div>    

{% endblock %}

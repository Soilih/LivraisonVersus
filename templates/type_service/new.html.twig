{% extends 'base.html.twig' %}

{% block title %}New TypeService{% endblock %}

{% block body %}
 <div id="right-panel" class="right-panel">
    <div class="content">
        <div class="animated fadeIn">
            <!-- Widgets  -->
            <div class="row">
                <div class="col-md-7">
                    <div class="card">
                        <div class="card-header bg-primary mb-3">
                            <strong class="card-title text-white">AJOUTER DES TYPES DES SERVICES   </strong>
                        </div>
                        <div class="card-body">
                             {{ include('type_service/_form.html.twig') }}
                            <br>
                            <a class="btn btn-success btn-sm" href="{{ path('type_service_index') }}">liste des type de services  </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
 </div>
</div>  
{% endblock %}
